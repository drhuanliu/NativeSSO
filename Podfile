use_frameworks!

abstract_target 'Mobile' do

  platform :ios, "13.0"
  target 'NativeSSO' do
  	pod 'OktaOidc'
    pod 'JWTDecode', '~> 2.6'
  end
end

abstract_target 'Mac' do

  platform :osx, "10.11"
  target 'NativeSSOMac' do
  	pod 'OktaOidc'
    pod 'JWTDecode', '~> 2.6'
  end
end
