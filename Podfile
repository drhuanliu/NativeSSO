use_frameworks!

abstract_target 'Mobile' do

  platform :ios, "13.0"
  target 'NativeSSO' do
  	pod 'OktaOidc'
  end
end

abstract_target 'Mac' do

  platform :osx, "10.10"
  target 'NativeSSOMac' do
  	pod 'OktaOidc'
  end
end
